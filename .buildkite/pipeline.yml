steps:
  - command: make install test
    label: ':go:'
    agents:
    - queue=central-dev

  - wait: ':shipit'

  - command: make install build publish/s3 publish/github
    label: ':shipit:'
    agents:
    - queue=central-prod